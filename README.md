# Pattern library starter project

Base pattern library project built using Fractal. http://fractal.build/

Requires:

Node v4.4.7+
Fractal CLI



`npm install`

To run in dev

`gulp`

To build static version

`gulp fractal:build`
